{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "lldb-debug",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/bin/examples/simple1.exe",
            "args": [
                "./test.micro",
                "-o",
                "./test.bin"
            ],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "build debug"
        }
    ]
}